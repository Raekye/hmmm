Int test_fn(Int x);

Int fib(Int x) {
	if (x <= 0) {
		0;
	} else {
		if (x == 1) {
			1;
		} else {
			fib(x - 1) + fib(x - 2);
		};
	};
};

Int loop(Int x) {
	if (x == 0) {
		0;
	} else {
		test_fn(x);
		loop(x - 1);
	};
};

Int n = 10;
fib(n) + loop(n);
